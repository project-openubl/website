---
title: API
hide_title: true
hide_table_of_contents: true
---

import useDocusaurusContext from "@docusaurus/useDocusaurusContext";
import { RedocStandalone } from "redoc";

export const ApiReference = () => {
  const { siteConfig } = useDocusaurusContext();
  React.useEffect(() => {
    const wrapper = document.getElementsByClassName(
      "docItemWrapper_node_modules-@docusaurus-theme-classic-lib-theme-DocItem-"
    )[0];
    if (wrapper) {
      wrapper.style.margin = 0;
      wrapper.style.setProperty("padding", "0", "important");
      wrapper.style.maxWidth = "97%";
    }
    const col = document.getElementsByClassName(
      "docItemContainer_node_modules-@docusaurus-theme-classic-lib-theme-DocItem-"
    )[0];
    if (col) {
      col.style.setProperty("padding", "0", "important");
    }
  }, []);
  return (
    <div>
      <RedocStandalone
        spec="../../openapi/searchpe.yaml"
        options={{
          theme: {
            colors: {
              primary: {
                main: "#2b9af3",
              },
            },
            typography: {
              fontSize: "var(--ifm-font-size-base)",
              lineHeight: "var(--ifm-line-height-base)",
              fontFamily: "var(--ifm-font-family-base)",
              headings: {
                fontFamily: "var(--ifm-font-family-base)",
                fontWeight: "var(--ifm-heading-font-weight)",
              },
              code: {
                lineHeight: "var(--ifm-pre-line-height)",
                fontFamily: "var(--ifm-font-family-monospace)",
              },
            },
            sidebar: {
              width: "300px", // about the same as the sidebar in the docs area, for consistency
            },
          },
        }}
      />
    </div>
  );
};

<ApiReference />
